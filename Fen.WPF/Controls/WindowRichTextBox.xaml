<Window x:Class="Fen.Wpf.Controls.WindowRichTextBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fen.Wpf.Controls"
        mc:Ignorable="d"
        Title="WindowRichTextBox" Height="450" Width="800">
    <Grid>
        <RichTextBox Name="richTB" VerticalScrollBarVisibility="Auto" IsEnabled="True">
            <FlowDocument>
                <Paragraph TextIndent="20">
                    将 HTML 内容粘贴到 可能会导致意外行为， RichTextBox 因为 RichTextBox 使用 RTF 格式而不是直接使用 HTML 格式。文本始终在 中 RichTextBox换行。
                    <LineBreak/> 如果不希望文本换行， PageWidth 请将 上的 FlowDocument 设置为大于 的 RichTextBox宽度。 但是，一旦达到页面宽度，文本仍会换行。
                </Paragraph>
                <Paragraph>
                   
                        <Label>1</Label>
                        <Label>2</Label>
                        <Label>3</Label>
                    <TextBox>abc</TextBox>
                </Paragraph>
                <!--块状容器只能加一个子节点-->
                <BlockUIContainer>
                    <StackPanel>
                        <CheckBox>1</CheckBox>
                        <CheckBox>2</CheckBox>
                        <CheckBox>3</CheckBox>
                    </StackPanel>
                </BlockUIContainer>
                <Paragraph FontSize="50">
                    <Run>x</Run>
                    <InlineUIContainer>
                        <Label Margin="-10,0,0,0" FontSize="30">4</Label>
                    </InlineUIContainer>
                    <Run>+</Run>
                    <Run>y</Run>
                    <InlineUIContainer>
                        <Label Margin="-10,0,0,0" FontSize="30">4</Label>
                    </InlineUIContainer>
                    <Run>=</Run>
                    <Run>99</Run>
                </Paragraph>

                <Paragraph FontSize="14" LineHeight="25">
                    <Run>这是一段包含超链接的文本：</Run>
                    <!-- 静态定义超链接，绑定点击事件 -->
                    <Hyperlink NavigateUri="https://www.baidu.com" 
                               Click="Hyperlink_OpenInBrowser"
                               Cursor="Hand"
                               Foreground="Blue" TextDecorations="Underline">
                        百度首页
                    </Hyperlink>
                    <Run>，点击即可打开浏览器。</Run>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>
    </Grid>
</Window>
