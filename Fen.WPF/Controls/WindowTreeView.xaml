<Window x:Class="Fen.Wpf.Controls.WindowTreeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fen.Wpf.Controls"
        xmlns:en="clr-namespace:Fen.Wpf.Entity"
        mc:Ignorable="d"
        Title="WindowTreeView" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TreeView Grid.Row="0">
            <TreeViewItem Header="节点1"></TreeViewItem>
            <TreeViewItem Header="节点2" IsExpanded="True">
                <TreeViewItem Header="节点2-1">
                    <TreeViewItem Header="节点2-1-1"></TreeViewItem>
                </TreeViewItem>
            </TreeViewItem>
            <TreeViewItem Header="节点3" IsExpanded="True">
                <TreeViewItem Header="节点3-1"></TreeViewItem>
                <TreeViewItem Header="节点3-2"></TreeViewItem>
            </TreeViewItem>
            <TreeViewItem Header="节点4"></TreeViewItem>
            <TreeViewItem Header="节点5"></TreeViewItem>
            <TreeViewItem Header="节点6"></TreeViewItem>
        </TreeView>
        <TreeView Grid.Row="1" Name="tree1" ItemsSource="{Binding}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type en:MenuInfo}" ItemsSource="{Binding SubMenus}">
                    <StackPanel Orientation="Horizontal">
                    <Image Source="/images/01.gif" />

                    <TextBlock Text="{Binding MenuName}"></TextBlock>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Window>
