<Window x:Class="Fen.Wpf.Controls.WindowProgressBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fen.Wpf.Controls"
        mc:Ignorable="d"
        Title="WindowProgressBar" Height="450" Width="800" Loaded="Window_Loaded"   >
    <Grid>
        <StackPanel>
            <ProgressBar Name="pro1" Foreground="Blue" Background="AliceBlue" Width="300" Height="70" Minimum="10" Maximum="100" IsIndeterminate="True">
                
            </ProgressBar>
            <ProgressBar Name="pro2" Foreground="Blue" Background="AliceBlue" Width="300" Height="70" Minimum="10" Maximum="100" IsIndeterminate="False">

            </ProgressBar>
            <ProgressBar Width="300" Height="60"
                 Name="progressBar1">
                <!--使用触发器Triggers 事件触发器 EventTrigger 使用路由事件绑定控件的加载事件-->
                <ProgressBar.Triggers>
                    <EventTrigger RoutedEvent="ProgressBar.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                Storyboard.TargetName="progressBar1" 
                Storyboard.TargetProperty="Value"
                From="0" To="100" Duration="0:0:5"  />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </ProgressBar.Triggers>
            </ProgressBar>
            <ProgressBar Width="300" Height="60"
     Name="progressBar2">
                <ProgressBar.Triggers>
                    <EventTrigger RoutedEvent="ProgressBar.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
    Storyboard.TargetName="progressBar2" 
    Storyboard.TargetProperty="Value"
    From="0" To="100" Duration="0:1:5"  />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </ProgressBar.Triggers>
            </ProgressBar>
        </StackPanel>
    </Grid>
</Window>
